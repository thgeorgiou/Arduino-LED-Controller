# Arduino LED Controller

This is a LED controller made with an Arduino. The LEDs are controlled over the network
using a normal HTTP server. This repository includes the arduino code and clients for
both Android and Windows.

For more info check out my website over [here](http://thgeorgiou.com/arduino-rgb-led-strip-controller/).